# Optional override: merge with base compose when the host exposes GPUs to Docker.
# Example: docker compose -f docker-compose.yml -f docker-compose.gpu.yml run --rm allinone
services:
  allinone:
    gpus: all
    environment:
      NVIDIA_VISIBLE_DEVICES: all
      NVIDIA_DRIVER_CAPABILITIES: compute,utility
